<UserControl x:Class="MiniERP.UI.Views.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource PrimaryBrush}">
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Form Panel -->
        <Border Grid.Column="0" Background="#243B53" CornerRadius="10" Padding="20" Margin="0,0,16,0">
            <StackPanel>
                <TextBlock Text="Kategori Bilgileri" Foreground="{StaticResource AccentBrush}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20"/>
                
                <TextBlock Style="{StaticResource FormLabel}" Text="Kategori Adı *"/>
                <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
                
                <TextBlock Style="{StaticResource FormLabel}" Text="Açıklama"/>
                <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                         TextWrapping="Wrap" AcceptsReturn="True" Height="80" VerticalContentAlignment="Top" Margin="0,0,0,20"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Style="{StaticResource SuccessButton}" Content="➕ Ekle" Command="{Binding AddCommand}" Width="80" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="✏️ Güncelle" Command="{Binding UpdateCommand}" Width="90" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource DangerButton}" Content="🗑️ Sil" Command="{Binding DeleteCommand}" Width="70" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource SecondaryButton}" Content="🔄" Command="{Binding ClearCommand}" Width="40"/>
                </StackPanel>
                
                <!-- Status -->
                <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource WarningBrush}" 
                           TextWrapping="Wrap" Margin="0,16,0,0" FontSize="12"/>
            </StackPanel>
        </Border>

        <!-- List -->
        <Border Grid.Column="1" Background="#243B53" CornerRadius="10" Padding="20">
            <StackPanel>
                <TextBlock Text="Kategori Listesi" Foreground="{StaticResource AccentBrush}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
                <DataGrid ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="Kategori Adı" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Açıklama" Binding="{Binding Description}" Width="*"/>
                        <DataGridTextColumn Header="Ürün Sayısı" Binding="{Binding ProductCount}" Width="90"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
