<UserControl x:Class="MiniERP.UI.Views.ProductListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource PrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top: Search + Form -->
        <Border Grid.Row="0" Background="#243B53" CornerRadius="10" Padding="20" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Ürün Bilgileri" Foreground="{StaticResource AccentBrush}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>
                
                <!-- Search -->
                <DockPanel Margin="0,0,0,16">
                    <Button DockPanel.Dock="Right" Style="{StaticResource PrimaryButton}" Content="🔍 Ara" 
                            Command="{Binding SearchCommand}" Width="80" Margin="8,0,0,0"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                </DockPanel>

                <!-- Form Grid -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Row 1 -->
                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,8">
                        <TextBlock Style="{StaticResource FormLabel}" Text="Ürün Adı *"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,8,8">
                        <TextBlock Style="{StaticResource FormLabel}" Text="Barkod"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding Barcode, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,8,8">
                        <TextBlock Style="{StaticResource FormLabel}" Text="Kategori *"/>
                        <ComboBox Style="{StaticResource ModernComboBox}" 
                                  ItemsSource="{Binding Categories}" 
                                  SelectedValue="{Binding SelectedCategoryId}"
                                  SelectedValuePath="Id" DisplayMemberPath="Name"/>
                    </StackPanel>
                    <StackPanel Grid.Row="0" Grid.Column="3" Margin="0,0,0,8">
                        <TextBlock Style="{StaticResource FormLabel}" Text="Açıklama"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <!-- Row 2 -->
                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,8">
                        <TextBlock Style="{StaticResource FormLabel}" Text="Alış Fiyatı (₺)"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding PurchasePrice, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,8,8">
                        <TextBlock Style="{StaticResource FormLabel}" Text="Satış Fiyatı (₺)"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding SalePrice, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,8,8">
                        <TextBlock Style="{StaticResource FormLabel}" Text="Başlangıç Stok"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding CurrentStock, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="3" Margin="0,0,0,8">
                        <TextBlock Style="{StaticResource FormLabel}" Text="Min. Stok Seviyesi"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding MinStockLevel, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <!-- Row 3: Buttons -->
                    <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
                        <Button Style="{StaticResource SuccessButton}" Content="➕ Ekle" Command="{Binding AddCommand}" Width="90" Margin="0,0,8,0"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="✏️ Güncelle" Command="{Binding UpdateCommand}" Width="100" Margin="0,0,8,0"/>
                        <Button Style="{StaticResource DangerButton}" Content="🗑️ Sil" Command="{Binding DeleteCommand}" Width="80" Margin="0,0,8,0"/>
                        <Button Style="{StaticResource SecondaryButton}" Content="🔄 Temizle" Command="{Binding ClearCommand}" Width="90"/>
                    </StackPanel>
                </Grid>
                
                <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource WarningBrush}" 
                           TextWrapping="Wrap" Margin="0,8,0,0" FontSize="12"/>
            </StackPanel>
        </Border>

        <!-- Product List -->
        <Border Grid.Row="1" Background="#243B53" CornerRadius="10" Padding="20">
            <StackPanel>
                <TextBlock Text="Ürün Listesi" Foreground="{StaticResource AccentBrush}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
                <DataGrid ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                        <DataGridTextColumn Header="Ürün Adı" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Barkod" Binding="{Binding Barcode}" Width="100"/>
                        <DataGridTextColumn Header="Kategori" Binding="{Binding CategoryName}" Width="100"/>
                        <DataGridTextColumn Header="Alış ₺" Binding="{Binding PurchasePrice, StringFormat='{}{0:N2}'}" Width="80"/>
                        <DataGridTextColumn Header="Satış ₺" Binding="{Binding SalePrice, StringFormat='{}{0:N2}'}" Width="80"/>
                        <DataGridTextColumn Header="Stok" Binding="{Binding CurrentStock}" Width="60"/>
                        <DataGridTextColumn Header="Min" Binding="{Binding MinStockLevel}" Width="50"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
