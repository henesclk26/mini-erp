<Window x:Class="MiniERP.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mini ERP - Stok Yönetim Sistemi" 
        Height="720" Width="1100"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource PrimaryBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#15202E" CornerRadius="0,0,0,0">
            <DockPanel>
                <!-- Logo / Title -->
                <Border DockPanel.Dock="Top" Padding="16,20" BorderBrush="#2A475E" BorderThickness="0,0,0,1">
                    <StackPanel>
                        <TextBlock Text="📦 Mini ERP" FontSize="20" FontWeight="Bold" 
                                   Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Text="Stok Yönetim Sistemi" FontSize="11" 
                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Navigation -->
                <StackPanel DockPanel.Dock="Top" Margin="8,12">
                    <TextBlock Text="ANA MENÜ" FontSize="10" FontWeight="SemiBold" 
                               Foreground="#5A7A9A" Margin="12,0,0,8"/>
                    
                    <Button Style="{StaticResource NavButton}" Content="🏠  Dashboard" 
                            Command="{Binding ShowDashboardCommand}"/>
                    
                    <TextBlock Text="YÖNETİM" FontSize="10" FontWeight="SemiBold"
                               Foreground="#5A7A9A" Margin="12,16,0,8"/>
                    
                    <Button Style="{StaticResource NavButton}" Content="📦  Ürünler" 
                            Command="{Binding ShowProductsCommand}"/>
                    <Button Style="{StaticResource NavButton}" Content="📁  Kategoriler" 
                            Command="{Binding ShowCategoriesCommand}"/>
                    
                    <TextBlock Text="STOK İŞLEMLERİ" FontSize="10" FontWeight="SemiBold"
                               Foreground="#5A7A9A" Margin="12,16,0,8"/>
                    
                    <Button Style="{StaticResource NavButton}" Content="📥  Stok Giriş" 
                            Command="{Binding ShowStockEntryCommand}"/>
                    <Button Style="{StaticResource NavButton}" Content="📤  Stok Çıkış" 
                            Command="{Binding ShowStockExitCommand}"/>
                    
                    <TextBlock Text="RAPORLAR" FontSize="10" FontWeight="SemiBold"
                               Foreground="#5A7A9A" Margin="12,16,0,8"/>
                    
                    <Button Style="{StaticResource NavButton}" Content="📊  Raporlar" 
                            Command="{Binding ShowReportsCommand}"/>
                </StackPanel>

                <!-- Footer -->
                <Border DockPanel.Dock="Bottom" Padding="16,12" BorderBrush="#2A475E" BorderThickness="0,1,0,0"
                        VerticalAlignment="Bottom">
                    <TextBlock Text="v1.0.0 © 2026" FontSize="10" Foreground="#5A7A9A" HorizontalAlignment="Center"/>
                </Border>
            </DockPanel>
        </Border>

        <!-- Main Content Area -->
        <DockPanel Grid.Column="1">
            <!-- Top Bar -->
            <Border DockPanel.Dock="Top" Background="#1A2D40" Padding="24,14" BorderBrush="#2A475E" BorderThickness="0,0,0,1">
                <TextBlock Text="{Binding CurrentViewTitle}" FontSize="18" FontWeight="SemiBold"
                           Foreground="White"/>
            </Border>

            <!-- Content -->
            <ContentControl Content="{Binding CurrentView}" Margin="0"/>
        </DockPanel>
    </Grid>
</Window>
