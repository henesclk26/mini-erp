<UserControl x:Class="MiniERP.UI.Views.StockEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource PrimaryBrush}">
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Entry Form -->
        <Border Grid.Column="0" Background="#243B53" CornerRadius="10" Padding="20" Margin="0,0,16,0">
            <StackPanel>
                <TextBlock Text="📥 Stok Giriş Formu" Foreground="{StaticResource SuccessBrush}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20"/>
                
                <TextBlock Style="{StaticResource FormLabel}" Text="Ürün Seçin *"/>
                <ComboBox Style="{StaticResource ModernComboBox}" 
                          ItemsSource="{Binding Products}"
                          SelectedValue="{Binding SelectedProductId}"
                          SelectedValuePath="Id" DisplayMemberPath="Name" Margin="0,0,0,12"/>
                
                <TextBlock Style="{StaticResource FormLabel}" Text="Miktar *"/>
                <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
                
                <TextBlock Style="{StaticResource FormLabel}" Text="Birim Fiyat (₺)"/>
                <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
                
                <TextBlock Style="{StaticResource FormLabel}" Text="Açıklama"/>
                <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                         TextWrapping="Wrap" AcceptsReturn="True" Height="60" VerticalContentAlignment="Top" Margin="0,0,0,20"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Style="{StaticResource SuccessButton}" Content="📥 Stok Girişi Yap" Command="{Binding SaveCommand}" Width="160" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource SecondaryButton}" Content="🔄 Temizle" Command="{Binding ClearCommand}" Width="100"/>
                </StackPanel>
                
                <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource WarningBrush}" 
                           TextWrapping="Wrap" Margin="0,16,0,0" FontSize="12"/>
            </StackPanel>
        </Border>

        <!-- Recent Entries -->
        <Border Grid.Column="1" Background="#243B53" CornerRadius="10" Padding="20">
            <StackPanel>
                <TextBlock Text="Son Stok Girişleri" Foreground="{StaticResource SuccessBrush}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
                <DataGrid ItemsSource="{Binding RecentEntries}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ürün" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="Miktar" Binding="{Binding Quantity}" Width="70"/>
                        <DataGridTextColumn Header="Birim Fiyat" Binding="{Binding UnitPrice, StringFormat='₺{0:N2}'}" Width="90"/>
                        <DataGridTextColumn Header="Toplam" Binding="{Binding TotalPrice, StringFormat='₺{0:N2}'}" Width="100"/>
                        <DataGridTextColumn Header="Tarih" Binding="{Binding MovementDate, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" Width="130"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
